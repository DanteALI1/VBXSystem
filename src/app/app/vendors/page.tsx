import { StubPage } from "@/components/app/stub-page";

export default function VendorsPage() {
  return (
    <StubPage
      title="Vendors"
      description="Vendor directory for affected products."
    />
  );
}
