import { StubPage } from "@/components/app/stub-page";

/** Minimal stub — full list/detail owned by Vulns agent. */
export default function VulnerabilitiesStubPage() {
  return (
    <StubPage
      title="Vulnerabilities"
      description="Catalog of CVE and BDU records. Full table and detail arrive from the Vulns track."
    />
  );
}
